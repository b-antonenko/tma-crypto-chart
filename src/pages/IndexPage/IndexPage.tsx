import type { FC } from 'react';
import { ChartComponent } from '../../components/Chart';

export const IndexPage: FC = ({ props }) => {
  return (
    <div>
      <ChartComponent />
    </div>
  );
};
